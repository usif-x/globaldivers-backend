# Nixpacks configuration for Top Divers Backend

[phases.setup]
aptPkgs = ["postgresql-client", "gcc", "python3-dev"]

[phases.install]
cmds = ["pip install --upgrade pip setuptools wheel", "pip install -r requirements.txt"]

[start]
cmd = "python main.py prod --workers 4"
